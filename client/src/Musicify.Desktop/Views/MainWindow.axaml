<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Musicify.Core.ViewModels"
        xmlns:v="using:Musicify.Desktop.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
        x:Class="Musicify.Desktop.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="{Binding CurrentProject.Name, StringFormat='{}{0} - Musicify'}"
        Width="1200" Height="800"
        MinWidth="1000" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA"
        Icon="/Assets/app-icon.png">

    <Design.DataContext>
        <vm:MainWindowViewModel />
    </Design.DataContext>

    <DockPanel>
        <!-- 菜单栏 -->
        <Menu DockPanel.Dock="Top" Background="#FFFFFF" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
            <MenuItem Header="文件(_F)">
                <MenuItem Header="保存(_S)" Command="{Binding SaveProjectCommand}" />
                <MenuItem Header="导出(_E)" />
                <Separator />
                <MenuItem Header="退出(_X)" />
            </MenuItem>
            <MenuItem Header="编辑(_E)">
                <MenuItem Header="撤销(_Z)" />
                <MenuItem Header="重做(_Y)" />
            </MenuItem>
            <MenuItem Header="视图(_V)">
                <MenuItem Header="项目概览" Command="{Binding NavigateToProjectOverviewCommand}" />
                <MenuItem Header="歌词编辑器" Command="{Binding NavigateToLyricsEditorCommand}" />
                <MenuItem Header="AI 对话" Command="{Binding NavigateToAIChatCommand}" />
            </MenuItem>
            <MenuItem Header="帮助(_H)">
                <MenuItem Header="关于 Musicify" />
            </MenuItem>
        </Menu>

        <!-- 工具栏 -->
        <Border DockPanel.Dock="Top" Background="#FAFAFA" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="10,5">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button Content="💾 保存" Command="{Binding SaveProjectCommand}" />
                <Button Content="📤 导出" />
                <Separator />
                <Button Content="⚙️ 设置" />
            </StackPanel>
        </Border>

        <!-- 主内容区域 -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 左侧边栏: 项目信息面板 -->
            <Border Grid.Column="0" 
                    Background="#FFFFFF" 
                    BorderBrush="#E5E7EB" 
                    BorderThickness="0,0,1,0">
                <ScrollViewer>
                    <StackPanel Margin="16" Spacing="16">
                        <!-- 项目信息卡片 -->
                        <Border Background="#F9FAFB" 
                                CornerRadius="8" 
                                Padding="16"
                                BorderBrush="#E5E7EB"
                                BorderThickness="1">
                            <StackPanel Spacing="12">
                                <TextBlock Text="项目信息" 
                                          FontSize="18" 
                                          FontWeight="Bold"
                                          Foreground="#1F2937" />
                                
                                <StackPanel Spacing="8">
                                    <TextBlock>
                                        <Run Text="名称: " FontWeight="Bold" Foreground="#4B5563" />
                                        <Run Text="{Binding ProjectSummary.ProjectName}" Foreground="#1F2937" />
                                    </TextBlock>
                                    
                                    <TextBlock>
                                        <Run Text="状态: " FontWeight="Bold" Foreground="#4B5563" />
                                        <Run Text="{Binding ProjectSummary.Status}" Foreground="#1F2937" />
                                    </TextBlock>
                                    
                                    <TextBlock>
                                        <Run Text="类型: " FontWeight="Bold" Foreground="#4B5563" />
                                        <Run Text="{Binding ProjectSummary.SongType}" Foreground="#1F2937" />
                                    </TextBlock>
                                    
                                    <TextBlock>
                                        <Run Text="创建时间: " FontWeight="Bold" Foreground="#4B5563" />
                                        <Run Text="{Binding ProjectSummary.CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Foreground="#1F2937" />
                                    </TextBlock>
                                    
                                    <TextBlock>
                                        <Run Text="更新时间: " FontWeight="Bold" Foreground="#4B5563" />
                                        <Run Text="{Binding ProjectSummary.UpdatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Foreground="#1F2937" />
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        
                        <!-- 导航菜单 -->
                        <Border Background="#F9FAFB" 
                                CornerRadius="8" 
                                Padding="16"
                                BorderBrush="#E5E7EB"
                                BorderThickness="1">
                            <StackPanel Spacing="8">
                                <TextBlock Text="导航" 
                                          FontSize="16" 
                                          FontWeight="Bold"
                                          Foreground="#1F2937"
                                          Margin="0,0,0,8" />
                                
                                <Button Content="📝 歌词编辑" 
                                       Command="{Binding NavigateToLyricsEditorCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Background="Transparent"
                                       BorderBrush="#E5E7EB"
                                       Padding="12,8" />
                                
                                <Button Content="🤖 AI 对话" 
                                       Command="{Binding NavigateToAIChatCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Background="Transparent"
                                       BorderBrush="#E5E7EB"
                                       Padding="12,8" />
                                
                                <Button Content="🎵 MIDI 分析" 
                                       Command="{Binding NavigateToMidiAnalysisCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Background="Transparent"
                                       BorderBrush="#E5E7EB"
                                       Padding="12,8" />
                                
                                <Button Content="⚙️ 项目设置" 
                                       Command="{Binding NavigateToProjectSettingsCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Background="Transparent"
                                       BorderBrush="#E5E7EB"
                                       Padding="12,8" />
                                
                                <Button Content="📤 导出歌词" 
                                       Command="{Binding NavigateToExportCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Background="Transparent"
                                       BorderBrush="#E5E7EB"
                                       Padding="12,8" />
                            </StackPanel>
                        </Border>

                        <!-- 加载状态 -->
                        <Border IsVisible="{Binding IsLoading}"
                                Background="#FEF3C7"
                                CornerRadius="8"
                                Padding="12">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="⏳" FontSize="16" />
                                <TextBlock Text="加载中..." Foreground="#92400E" />
                            </StackPanel>
                        </Border>

                        <!-- 错误消息 -->
                        <Border IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                Background="#FEE2E2"
                                CornerRadius="8"
                                Padding="12">
                            <StackPanel Spacing="4">
                                <TextBlock Text="❌ 错误" FontWeight="Bold" Foreground="#991B1B" />
                                <TextBlock Text="{Binding ErrorMessage}" 
                                          Foreground="#991B1B"
                                          TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            
            <!-- 右侧内容区域 -->
            <ContentControl Grid.Column="1" 
                           Background="#FFFFFF"
                           Name="ContentArea" />
        </Grid>
    </DockPanel>
</Window>
