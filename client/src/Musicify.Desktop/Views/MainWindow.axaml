<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Musicify.Core.ViewModels"
        xmlns:v="using:Musicify.Desktop.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
        x:Class="Musicify.Desktop.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="{Binding CurrentProject.Name, StringFormat='{}{0} - Musicify'}"
        Width="1200" Height="800"
        MinWidth="1000" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA"
        Icon="/Assets/app-icon.png">

    <Design.DataContext>
        <vm:MainWindowViewModel />
    </Design.DataContext>

    <DockPanel>
        <!-- 顶部导航栏 -->
        <Border DockPanel.Dock="Top"
                Background="#1E1E1E"
                Padding="16,12"
                BorderBrush="#374151"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo 和应用标题 -->
                <StackPanel Grid.Column="0"
                           Orientation="Horizontal"
                           Spacing="12"
                           VerticalAlignment="Center">
                    <Border Background="#6366F1"
                           CornerRadius="6"
                           Padding="8"
                           Width="32"
                           Height="32">
                        <TextBlock Text="🎵"
                                  FontSize="16"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Musicify"
                                  FontSize="18"
                                  FontWeight="SemiBold"
                                  Foreground="White"/>
                        <TextBlock Text="{Binding CurrentProject.Name, FallbackValue='新项目', StringFormat='{}{0}'}"
                                  FontSize="12"
                                  Foreground="#D1D5DB"
                                  Margin="0,-2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- 中间部分（可扩展） -->
                <StackPanel Grid.Column="1"
                           Orientation="Horizontal"
                           Spacing="8"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <Border Background="#374151"
                           CornerRadius="20"
                           Padding="12,6">
                        <TextBlock Text="🎵 项目管理"
                                  FontSize="14"
                                  Foreground="White"
                                  FontWeight="Medium"/>
                    </Border>
                </StackPanel>

                <!-- 右侧工具按钮 -->
                <StackPanel Grid.Column="2"
                           Orientation="Horizontal"
                           Spacing="8"
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center">
                    <Button Classes="icon"
                           Command="{Binding SaveProjectCommand}"
                           ToolTip.Tip="保存项目 (Ctrl+S)">
                        <TextBlock Text="💾" FontSize="14"/>
                    </Button>
                    <Button Classes="icon"
                           Command="{Binding NavigateToExportCommand}"
                           ToolTip.Tip="导出项目">
                        <TextBlock Text="📤" FontSize="14"/>
                    </Button>
                    <Button Classes="icon"
                           Command="{Binding NavigateToProjectSettingsCommand}"
                           ToolTip.Tip="项目设置">
                        <TextBlock Text="⚙️" FontSize="14"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 左侧边栏: 项目信息面板 -->
            <Border Grid.Column="0"
                    Background="#1F2937"
                    BorderBrush="#374151"
                    BorderThickness="0,0,1,0">
                <ScrollViewer>
                    <StackPanel Margin="16" Spacing="16">
                        <!-- 项目信息卡片 -->
                        <Border Background="#374151"
                                CornerRadius="12"
                                Padding="16"
                                BorderBrush="#4B5563"
                                BorderThickness="1">
                            <StackPanel Spacing="12">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="📋" FontSize="16"/>
                                    <TextBlock Text="项目信息"
                                              FontSize="16"
                                              FontWeight="SemiBold"
                                              Foreground="White" />
                                </StackPanel>

                                <StackPanel Spacing="12">
                                    <StackPanel>
                                        <TextBlock Text="名称"
                                                  FontSize="12"
                                                  Foreground="#D1D5DB"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding ProjectSummary.ProjectName, FallbackValue='-', TargetNullValue='-'}"
                                                  FontSize="14"
                                                  Foreground="White"
                                                  FontWeight="Medium"/>
                                    </StackPanel>

                                    <StackPanel>
                                        <TextBlock Text="状态"
                                                  FontSize="12"
                                                  Foreground="#D1D5DB"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding ProjectSummary.Status, FallbackValue='-', TargetNullValue='-'}"
                                                  FontSize="14"
                                                  Foreground="White"
                                                  FontWeight="Medium"/>
                                    </StackPanel>

                                    <StackPanel>
                                        <TextBlock Text="类型"
                                                  FontSize="12"
                                                  Foreground="#D1D5DB"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding ProjectSummary.SongType, FallbackValue='-', TargetNullValue='-'}"
                                                  FontSize="14"
                                                  Foreground="White"
                                                  FontWeight="Medium"/>
                                    </StackPanel>

                                    <StackPanel>
                                        <TextBlock Text="创建时间"
                                                  FontSize="12"
                                                  Foreground="#D1D5DB"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding ProjectSummary.CreatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}', FallbackValue='-', TargetNullValue='-'}"
                                                  FontSize="14"
                                                  Foreground="White"
                                                  FontWeight="Medium"/>
                                    </StackPanel>

                                    <StackPanel>
                                        <TextBlock Text="更新时间"
                                                  FontSize="12"
                                                  Foreground="#D1D5DB"
                                                  Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding ProjectSummary.UpdatedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}', FallbackValue='-', TargetNullValue='-'}"
                                                  FontSize="14"
                                                  Foreground="White"
                                                  FontWeight="Medium"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- 导航菜单 -->
                        <Border Background="#374151"
                                CornerRadius="12"
                                Padding="16"
                                BorderBrush="#4B5563"
                                BorderThickness="1">
                            <StackPanel Spacing="8">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="🧭" FontSize="16"/>
                                    <TextBlock Text="导航"
                                              FontSize="16"
                                              FontWeight="SemiBold"
                                              Foreground="White"
                                              Margin="0,0,0,8" />
                                </StackPanel>

                                <Button Classes="secondary"
                                       Command="{Binding NavigateToLyricsEditorCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Padding="12,10"
                                       Margin="0,2">
                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <TextBlock Text="📝" FontSize="16"/>
                                        <TextBlock Text="歌词编辑"
                                                  FontSize="14"
                                                  FontWeight="Medium"/>
                                    </StackPanel>
                                </Button>

                                <Button Classes="secondary"
                                       Command="{Binding NavigateToAIChatCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Padding="12,10"
                                       Margin="0,2">
                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <TextBlock Text="🤖" FontSize="16"/>
                                        <TextBlock Text="AI 对话"
                                                  FontSize="14"
                                                  FontWeight="Medium"/>
                                    </StackPanel>
                                </Button>

                                <Button Classes="secondary"
                                       Command="{Binding NavigateToMidiAnalysisCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Padding="12,10"
                                       Margin="0,2">
                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <TextBlock Text="🎵" FontSize="16"/>
                                        <TextBlock Text="MIDI 分析"
                                                  FontSize="14"
                                                  FontWeight="Medium"/>
                                    </StackPanel>
                                </Button>

                                <Button Classes="secondary"
                                       Command="{Binding NavigateToProjectSettingsCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Padding="12,10"
                                       Margin="0,2">
                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <TextBlock Text="⚙️" FontSize="16"/>
                                        <TextBlock Text="项目设置"
                                                  FontSize="14"
                                                  FontWeight="Medium"/>
                                    </StackPanel>
                                </Button>

                                <Button Classes="secondary"
                                       Command="{Binding NavigateToExportCommand}"
                                       HorizontalAlignment="Stretch"
                                       HorizontalContentAlignment="Left"
                                       Padding="12,10"
                                       Margin="0,2">
                                    <StackPanel Orientation="Horizontal" Spacing="12">
                                        <TextBlock Text="📤" FontSize="16"/>
                                        <TextBlock Text="导出歌词"
                                                  FontSize="14"
                                                  FontWeight="Medium"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- 加载状态 -->
                        <Border IsVisible="{Binding IsLoading}"
                                Background="#F59E0B"
                                CornerRadius="12"
                                Padding="12">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="⏳" FontSize="16" />
                                <TextBlock Text="加载中..." Foreground="White" FontWeight="Medium"/>
                            </StackPanel>
                        </Border>

                        <!-- 错误消息 -->
                        <Border IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                Background="#EF4444"
                                CornerRadius="12"
                                Padding="12">
                            <StackPanel Spacing="4">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="❌" FontSize="16" FontWeight="Bold"/>
                                    <TextBlock Text="错误" FontWeight="Bold" Foreground="White" />
                                </StackPanel>
                                <TextBlock Text="{Binding ErrorMessage}"
                                          Foreground="White"
                                          TextWrapping="Wrap"
                                          FontSize="12"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- 右侧内容区域 -->
            <Border Grid.Column="1"
                    Background="#F3F4F6">
                <ContentControl Name="ContentArea"
                               Background="Transparent"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>
